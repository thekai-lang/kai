[package]
name = "kai-ownership"
version.workspace = true
edition.workspace = true

[dependencies]
kai-tast = { path = "../kai-tast" }

[dev-dependencies]
kai-diagnostics = { path = "../kai-diagnostics" }
